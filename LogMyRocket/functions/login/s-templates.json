{
  "apiLoginPostTemplate": {
    "application/json": {
      "request": {
        "http_method": "$context.httpMethod",
        "path": "$context.resourcePath"
      },
      "username": "$input.json('$.username')",
      "password": "$input.json('$.password')"
    }
  }
}